status: rejected
attempt: 1
feedback:
  - "Falta el origen de `fecha_creacion` para el objeto `lead` que consume `buildClinicNotificationEmail(lead)`. El diseño exige `{ id, nombre, email, telefono, servicio, mensaje, fecha_creacion }`, pero el `INSERT` real en `api/leads.js` (línea 665) hace `.select('id')` — solo trae `id`, no `fecha_creacion` (ese valor lo genera el default `now()` de Postgres y hoy nunca vuelve al handler). El spec debe decidir explícitamente una de dos rutas y dejarlo escrito: (a) cambiar esa consulta a `.select('id, fecha_creacion')` para traer el valor real generado por la base, o (b) generar el timestamp localmente (ej. `new Date().toISOString()`) antes/después del insert y usar ese valor consistente en el correo. Sin esa decisión, dos implementaciones razonables (con o sin una columna extra en el `.select()`) producirían resultados distintos y ningún criterio de aceptación lo detectaría."
  - "El criterio de aceptación 3 (el único que especifica qué campos del HTML se testean) omite `fecha_creacion` e `id`, aunque la sección 'Diseño propuesto → Contenido mínimo requerido' exige ambos explícitamente ('Fecha de recepción... e `id` del lead'). Esto es una inconsistencia real entre lo que el diseño manda implementar y lo que los criterios de aceptación obligan a testear: un `qa-agent` podría dar por aprobada la feature sin verificar nunca que la fecha o el id aparezcan en el HTML. Agregar `fecha_creacion` e `id` al criterio 3 (o crear un criterio nuevo) para que sea verificable de punta a punta."
  - "La mitigación de header injection en el `subject` (saneamiento de `\r`/`\n` provenientes de `nombre`) está bien descrita y justificada en 'Casos borde a contemplar' como 'riesgo de seguridad real', pero no existe ningún criterio de aceptación numerado (1-18) que exija un test que la verifique — a diferencia del escapado HTML (criterio 3) o del contador de `sendMail()` (criterio 4), que sí son criterios explícitos y verificables. Agregar un criterio de aceptación explícito: inyectar `nombre` con `\r\n` (ej. simulando `\r\nBcc: attacker@evil.com`) y confirmar que el `subject` resultante pasado a `sendMail()` no contiene saltos de línea crudos."

## Notas adicionales (no bloqueantes, no requieren cambio)

- El patrón de inyección (`supabaseClientFactory`, análogo a `mailerFactory`) descrito en el spec coincide exactamente con `api/leads.js` real (líneas 341-357); no es una invención.
- `escapeHtml()` en `api/_lib/sanitize-html.js` es real, ya deja explícitamente documentado en su comentario que está lista para las features `05`/`06`, y su comportamiento (no toca `\r`/`\n`) coincide con lo que el spec asume en "Casos borde".
- La columna `notificacion_clinica_enviada` ya existe en la migración (`supabase/migrations/20260819210130_create_leads_table.sql`, `default false`), consistente con "no se agrega ninguna columna nueva".
- `LEADS_NOTIFICATION_EMAIL`, `SMTP_HOST/PORT/USER/PASS/FROM` ya están en `.env.example` desde la feature 01, consistente con "no se agregan variables nuevas".
- Los criterios de documentación (`docs/tecnica/<slug>.md`, `docs/usuario/<slug>.md`), `decision.md` (criterio 16) y los enlaces exactos en ambos índices (criterios 17/18, con el texto exacto usado por `update-doc-indexes.ps1`) están presentes — no aplica el rechazo automático por documentación.
- No hay contenido médico/clínico inventado: el spec prohíbe explícitamente marketing/precios/tratamientos/certificaciones en el email y solo usa el nombre de negocio ya establecido ("Sonríe más").
- El manejo de errores SMTP (try/catch acotado, nunca toca el catch genérico de 500, nunca altera el 201, nunca duplica/borra el lead) es consistente con el patrón ya usado en el código real para el chequeo de duplicados y el insert (bloques try/catch aislados con su propio `respond()`).
- `EMAIL_PATTERN` (`^[^\s@]+@[^\s@]+\.[^\s@]+$`) ya excluye cualquier whitespace, incluido `\r`/`\n`, por lo que usar el `email` del lead como `replyTo` sin escapar no representa riesgo de header injection — la objeción que el propio spec invita a hacer sobre esto no amerita rechazo.

Archivos revisados: `api/leads.js`, `api/_lib/supabase-client.js`, `api/_lib/sanitize-html.js`, `api/leads.test.js`, `supabase/migrations/20260819210130_create_leads_table.sql`, `.env.example`, `docs/tecnica/arquitectura.md`, `docs/tecnica/endpoint-recepcion-leads.md`, `docs/tecnica/index.md`, `docs/usuario/index.md`, `package.json`, `ROADMAP.md`.
