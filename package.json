{
  "name": "gi-clinicadental",
  "version": "1.0.1",
  "private": true,
  "description": "Backend Serverless (Vercel + Supabase) para el sitio de captacion de leads de gi-clinicadental.",
  "scripts": {
    "build:site": "node scripts/build-site.js",
    "check:site": "node scripts/build-site.js --check",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "headroom-ai": "^0.37.0",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
